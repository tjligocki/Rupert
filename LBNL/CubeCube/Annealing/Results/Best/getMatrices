#!/bin/csh -f
foreach i (best.vertices.raw/*)
  set basename=`basename $i`
  echo $basename
  set n=$basename:e
  set rest=$basename:r
  set m=$rest:e
  awk -v n=$n -v m=$m -f getMatrices.awk $i > best.matrices/run.$m.$n
end
